# urlTest

## How to run project

### write in terminal :  
- `cd frontendPart`
- `yarn install`
- `yarn build`

## then set up `pipenv environment` 

### write in terminal : 
- `cd ..`
- `cd backend`
- `docker compose up --build`

